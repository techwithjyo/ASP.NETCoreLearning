@model System.Collections.Generic.List<WeatherApp.Models.CityWeather>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>title</h2>

@foreach (var i in Model)
{
    <p>@i.CityUniqueCode | @i.CityName | @i.DateAndTime | @i.TemperatureFarhenheit</p>
        
}
